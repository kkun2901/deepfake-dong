# 튜닝 후 프로젝트 용량 가이드

## 📊 용량 증가 계산

### 1. 모델 가중치 파일들 (.pth)

Meso4는 매우 작은 모델입니다:

| 파일 | 크기 | 설명 |
|------|------|------|
| `mesonet_best.pth` | ~1-2MB | 최고 성능 모델 |
| `mesonet_final.pth` | ~1-2MB | 최종 모델 |
| `mesonet_epoch_5.pth` | ~1-2MB | 체크포인트 (5 에포크) |
| `mesonet_epoch_10.pth` | ~1-2MB | 체크포인트 (10 에포크) |
| `mesonet_epoch_15.pth` | ~1-2MB | 체크포인트 (15 에포크) |
| `mesonet_epoch_20.pth` | ~1-2MB | 체크포인트 (20 에포크) |

**총 모델 파일 용량**: 약 **6-12MB** (20 에포크 기준)

### 2. 데이터셋

| 데이터셋 크기 | 실제 디스크 용량 |
|-------------|----------------|
| 500MB | ~500MB |
| 1GB | ~1GB |
| 1.5GB | ~1.5GB |
| 2GB | ~2GB |

### 3. 기타 파일

- 로그 파일: ~1-5MB (매우 작음)
- 임시 파일: ~10-50MB (학습 중)

## 💾 총 용량 증가

### 최소 구성 (500MB 데이터셋)
- 모델 파일: ~6MB
- 데이터셋: ~500MB
- 기타: ~20MB
- **총 증가**: 약 **530MB**

### 권장 구성 (1-1.5GB 데이터셋)
- 모델 파일: ~10MB
- 데이터셋: ~1-1.5GB
- 기타: ~30MB
- **총 증가**: 약 **1-1.5GB**

### 최대 구성 (2GB 데이터셋)
- 모델 파일: ~12MB
- 데이터셋: ~2GB
- 기타: ~50MB
- **총 증가**: 약 **2.1GB**

## 🔒 Git 저장소 용량

**중요**: `.gitignore`에 이미 다음이 포함되어 있습니다:

```
# Model weights (large files)
backend/weights/
*.pth
*.h5
*.pt
*.ckpt
```

따라서:
- ✅ **모델 파일들은 Git에 올라가지 않음** (로컬에만 저장)
- ✅ **데이터셋도 Git에 올라가지 않음** (별도 관리 필요)
- ✅ **Git 저장소 크기는 거의 안 늘어남**

## 📁 용량 관리 팁

### 1. 불필요한 체크포인트 삭제

학습 완료 후 최고 모델만 남기기:

```bash
cd backend/weights

# 최고 모델만 남기고 나머지 삭제
rm mesonet_epoch_*.pth  # 체크포인트 삭제
# mesonet_best.pth와 mesonet_final.pth만 남김
```

**절약**: 약 4-8MB

### 2. 데이터셋 압축

데이터셋을 사용하지 않을 때 압축:

```bash
# 데이터셋 압축
tar -czf dataset_backup.tar.gz dataset/

# 압축 해제
tar -xzf dataset_backup.tar.gz
```

**절약**: 약 30-50% (압축률에 따라)

### 3. 데이터셋을 외부 저장소에 보관

- 외장 하드디스크
- 클라우드 스토리지 (Google Drive, Dropbox 등)
- NAS

## 📊 실제 사용 시나리오

### 시나리오 1: 빠른 테스트 (500MB 데이터셋)
```
로컬 디스크 증가: ~530MB
Git 저장소 증가: ~0MB (무시됨)
```

### 시나리오 2: 본격 학습 (1.5GB 데이터셋)
```
로컬 디스크 증가: ~1.5GB
Git 저장소 증가: ~0MB (무시됨)
```

### 시나리오 3: 여러 번 학습 (체크포인트 보관)
```
로컬 디스크 증가: ~2-3GB (체크포인트 포함)
Git 저장소 증가: ~0MB (무시됨)
```

## ✅ 결론

1. **로컬 디스크**: 약 **1-2GB** 증가 (데이터셋 크기에 따라)
2. **Git 저장소**: **거의 안 늘어남** (`.gitignore`로 제외)
3. **모델 파일**: 매우 작음 (**6-12MB**)
4. **데이터셋**: 가장 큰 용량 차지 (**1-2GB**)

**권장사항**:
- 데이터셋은 외부 저장소에 보관
- 학습 완료 후 불필요한 체크포인트 삭제
- 최종 모델만 프로젝트에 포함 (필요시)



